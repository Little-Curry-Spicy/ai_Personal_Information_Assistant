# 前端部署子路径，默认 /；如部署到 https://domain/info 则设为 /info/
VITE_APP_BASE=/

# 简历入库是否启用「覆盖同名分块」：true（默认）显示侧栏开关且默认勾选；false 隐藏开关、上传固定为追加模式
# VITE_RESUME_INGEST_OVERWRITE=false

# 侧栏「知识库更新」整块（上传简历 + GitHub 同步）：false 时整卡不展示
# VITE_SHOW_KNOWLEDGE_PANEL=false